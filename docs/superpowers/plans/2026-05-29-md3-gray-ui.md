# MD3 淡灰色系 UI 重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 MusicClassifier 界面从 Apple 风格重构为 Material Design 3 淡灰色系风格

**Architecture:** 纯 QSS 样式变更 + 图标按钮绘制参数调整，不改变任何功能逻辑或布局结构。所有变更集中在 `gui/` 目录下的 5 个文件。

**Tech Stack:** PySide6, QPainter (图标绘制)

---

### Task 1: 更新主窗口 QSS 样式表

**Files:**
- Modify: `gui/main_window.py:105-153` (LIGHT_FLAT_QSS)

- [ ] **Step 1: 替换 LIGHT_FLAT_QSS 常量**

将 `gui/main_window.py` 中的 `LIGHT_FLAT_QSS` 常量替换为 MD3 淡灰色系版本：

```python
LIGHT_FLAT_QSS = """
QMainWindow {
    background: #fafafa;
}
QWidget#outer {
    background: #fafafa;
}
QWidget#sidebar {
    background: #fafafa;
}
QWidget#track_card {
    background: #ffffff;
    border: none;
    border-radius: 12px;
}
QLabel#track_name {
    font-size: 13px;
    font-weight: 500;
    color: #202124;
}
QLabel#track_subtitle {
    font-size: 11px;
    color: #80868b;
}
QLabel#volume_tag {
    font-size: 10px;
    color: #5f6368;
    font-weight: 600;
    letter-spacing: 0.5px;
}
QLabel#tag_header {
    font-size: 9px;
    font-weight: 600;
    color: #5f6368;
    letter-spacing: 1px;
}
QLabel#volume_label {
    font-size: 11px;
    font-weight: 600;
    color: #202124;
}
QPushButton#playlist_btn {
    background-color: #e8eaed;
    color: #202124;
    border: none;
    border-radius: 8px;
    padding: 6px 4px;
    font-size: 11px;
    min-height: 28px;
}
QPushButton#playlist_btn:hover {
    background-color: #dadce0;
}
QPushButton#playlist_btn:pressed {
    background-color: #c4c7c9;
}
QPushButton#playlist_btn:disabled {
    background-color: #f1f3f4;
    color: #9aa0a6;
}

"""
```

- [ ] **Step 2: 运行测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 全部通过

---

### Task 2: 更新侧边栏样式和布局间距

**Files:**
- Modify: `gui/main_window.py:207-223` (sidebar section)
- Modify: `gui/main_window.py:253-254` (grid spacing)

- [ ] **Step 1: 移除侧边栏边框和调整内边距**

在 `_init_ui` 方法中，侧边栏 `QWidget` 的 `setObjectName("sidebar")` 保持不变（QSS 已移除 border-right），调整内边距：

将：
```python
sidebar_layout.setContentsMargins(6, 8, 6, 8)
sidebar_layout.setSpacing(6)
```

替换为：
```python
sidebar_layout.setContentsMargins(10, 8, 10, 8)
sidebar_layout.setSpacing(6)
```

- [ ] **Step 2: 更新网格间距**

将：
```python
grid.setSpacing(2)
```

替换为：
```python
grid.setSpacing(4)
```

- [ ] **Step 3: 更新曲目卡片内边距和固定高度**

将：
```python
track_card_layout.setContentsMargins(8, 2, 8, 2)
```

替换为：
```python
track_card_layout.setContentsMargins(10, 4, 10, 4)
```

将：
```python
track_card.setFixedHeight(52)
```

替换为：
```python
track_card.setFixedHeight(56)
```

- [ ] **Step 4: 运行测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 全部通过

---

### Task 3: 更新图标按钮为 MD3 Filled Tonal 风格

**Files:**
- Modify: `gui/main_window.py:77-103` (_make_circle_icon 函数)
- Modify: `gui/main_window.py:66-74` (图标路径常量)

- [ ] **Step 1: 更新图标路径常量**

将播放图标从描边三角形改为填充三角形，设置图标保持三条横线不变：

```python
_PLAY_ICON_PATHS = [
    "M 6 4 L 18 12 L 6 20 Z"
]

_SETTINGS_ICON_PATHS = [
    "M 5 10 L 19 10",
    "M 5 14 L 19 14",
    "M 5 18 L 19 18"
]
```

- [ ] **Step 2: 更新 _draw_svg_icon 函数**

将描边宽度从 2 改为 1.5，并支持填充模式：

```python
def _draw_svg_icon(size: int, color: str, paths: list[str], fill: bool = False) -> QIcon:
    pixmap = QPixmap(size, size)
    pixmap.fill(Qt.GlobalColor.transparent)
    painter = QPainter(pixmap)
    painter.setRenderHint(QPainter.RenderHint.Antialiasing)
    pen = QPen(QColor(color))
    pen.setWidth(1.5)
    pen.setCapStyle(Qt.PenCapStyle.RoundCap)
    pen.setJoinStyle(Qt.PenJoinStyle.RoundJoin)
    painter.setPen(pen)
    if fill:
        painter.setBrush(QColor(color))
    else:
        painter.setBrush(Qt.BrushStyle.NoBrush)
    for path_data in paths:
        from PySide6.QtGui import QPainterPath
        path = QPainterPath()
        parts = path_data.split()
        i = 0
        while i < len(parts):
            cmd = parts[i]
            if cmd == 'M':
                path.moveTo(float(parts[i+1]), float(parts[i+2]))
                i += 3
            elif cmd == 'L':
                path.lineTo(float(parts[i+1]), float(parts[i+2]))
                i += 3
            elif cmd == 'C':
                path.cubicTo(float(parts[i+1]), float(parts[i+2]),
                             float(parts[i+3]), float(parts[i+4]),
                             float(parts[i+5]), float(parts[i+6]))
                i += 7
            elif cmd == 'A':
                cx, cy, rx, ry, start, sweep = float(parts[i+1]), float(parts[i+2]), float(parts[i+3]), float(parts[i+4]), float(parts[i+5]), float(parts[i+6])
                path.arcTo(cx-rx, cy-ry, rx*2, ry*2, start, sweep)
                i += 7
            elif cmd == 'Z':
                path.closeSubpath()
                i += 1
            else:
                i += 1
        painter.drawPath(path)
    painter.end()
    return QIcon(pixmap)
```

- [ ] **Step 3: 重写 _make_circle_icon 函数**

```python
def _make_circle_icon(icon_type: str, color: str, parent=None) -> QPushButton:
    btn = QPushButton(parent)
    btn.setFixedSize(28, 28)
    fill = icon_type == "play"
    icon = _draw_svg_icon(18, color, _PLAY_ICON_PATHS if icon_type == "play" else _SETTINGS_ICON_PATHS, fill=fill)
    btn.setIcon(icon)
    btn.setIconSize(QSize(18, 18))
    btn.setStyleSheet(f"""
        QPushButton {{
            background-color: #e0e0e0;
            border: none;
            border-radius: 14px;
            padding: 0px;
        }}
        QPushButton:hover {{
            background-color: #dadce0;
        }}
        QPushButton:pressed {{
            background-color: #c4c7c9;
        }}
    """)
    return btn
```

- [ ] **Step 4: 更新 _on_start_toggle 中的按钮状态切换逻辑**

当前代码调用 `self._start_btn.setColor("#007aff")` 和 `self._start_btn.setColor("#ff3b30")`，但 QPushButton 没有 `setColor` 方法。需要改为通过 QSS 切换样式：

将 `self._start_btn.setColor("#007aff")` 替换为：
```python
self._start_btn.setStyleSheet("""
    QPushButton {
        background-color: #e0e0e0;
        border: none;
        border-radius: 14px;
        padding: 0px;
    }
    QPushButton:hover {
        background-color: #dadce0;
    }
    QPushButton:pressed {
        background-color: #c4c7c9;
    }
""")
```

将 `self._start_btn.setColor("#ff3b30")` 替换为：
```python
self._start_btn.setStyleSheet("""
    QPushButton {
        background-color: #c4c7c9;
        border: none;
        border-radius: 14px;
        padding: 0px;
    }
    QPushButton:hover {
        background-color: #bdc1c4;
    }
    QPushButton:pressed {
        background-color: #b0b3b7;
    }
""")
```

同样处理 `_handle_error` 中的 `self._start_btn.setColor("#007aff")`。

- [ ] **Step 5: 移除 _on_start_toggle 中的 setText 调用**

当前代码有 `self._start_btn.setText("▶")` 和 `self._start_btn.setText("■")`，但按钮已使用 QIcon，不需要文字。删除这两行。

同样处理 `_handle_error` 中的 `self._start_btn.setText("▶")`。

- [ ] **Step 6: 运行测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 全部通过

---

### Task 4: 更新 IconButton 类样式

**Files:**
- Modify: `gui/icon_button.py`

- [ ] **Step 1: 更新 IconButton 的 QSS 样式为 MD3 淡灰色系**

```python
from PySide6.QtWidgets import QToolButton
from PySide6.QtCore import Qt
from PySide6.QtGui import QColor


class IconButton(QToolButton):
    def __init__(self, parent=None, color="#5f6368", icon_text=""):
        super().__init__(parent)
        self._base_color = QColor(color)
        self._icon_text = icon_text
        self.setFixedSize(28, 28)
        self.setCursor(Qt.PointingHandCursor)
        self.setText(icon_text)
        self.setStyleSheet(self._build_qss(color))

    def _build_qss(self, color: str) -> str:
        return f"""
            QToolButton {{
                background-color: #e0e0e0;
                color: {color};
                border: none;
                border-radius: 14px;
                padding: 0px;
                font-size: 14px;
                font-weight: 500;
            }}
            QToolButton:hover {{
                background-color: #dadce0;
            }}
            QToolButton:pressed {{
                background-color: #c4c7c9;
            }}
            QToolButton:disabled {{
                background-color: #f1f3f4;
                color: #9aa0a6;
            }}
        """

    def setColor(self, color: str):
        self._base_color = QColor(color)
        self.setStyleSheet(self._build_qss(color))
```

- [ ] **Step 2: 运行测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 全部通过

---

### Task 5: 更新 SettingsPopover 样式

**Files:**
- Modify: `gui/settings_popover.py`

- [ ] **Step 1: 更新容器和菜单项样式**

将容器样式从：
```python
self._container.setStyleSheet(
    "background-color: #ffffff; border: 1px solid rgba(0, 0, 0, 0.08); border-radius: 10px;"
)
```

替换为：
```python
self._container.setStyleSheet(
    "background-color: #ffffff; border: none; border-radius: 12px;"
)
```

将菜单项默认样式从：
```python
"padding: 0 12px; font-size: 12px; color: #1d1d1f; border-radius: 6px;"
```

替换为：
```python
"padding: 0 12px; font-size: 12px; color: #202124; border-radius: 8px;"
```

将悬停样式从：
```python
"padding: 0 12px; font-size: 12px; color: #1d1d1f;"
" border-radius: 6px; background-color: #f2f2f7;"
```

替换为：
```python
"padding: 0 12px; font-size: 12px; color: #202124;"
" border-radius: 8px; background-color: #e8eaed;"
```

将离开样式从：
```python
"padding: 0 12px; font-size: 12px; color: #1d1d1f; border-radius: 6px;"
```

替换为：
```python
"padding: 0 12px; font-size: 12px; color: #202124; border-radius: 8px;"
```

- [ ] **Step 2: 更新阴影颜色**

将 `paintEvent` 中的阴影颜色从 `QColor(0, 0, 0, 15)` 替换为 `QColor(0, 0, 0, 13)`。

- [ ] **Step 3: 运行测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 全部通过

---

### Task 6: 更新 CaptureWizard 样式

**Files:**
- Modify: `gui/capture_wizard.py`

- [ ] **Step 1: 更新对话框 QSS**

将：
```python
self.setStyleSheet("""
    QDialog {
        background-color: #f3f3f3;
    }
    QLabel {
        color: #1d1d1f;
    }
    QProgressBar {
        border: none;
        border-radius: 4px;
        background-color: #e5e5ea;
        text-align: center;
        color: #1d1d1f;
    }
    QProgressBar::chunk {
        background-color: #007aff;
        border-radius: 4px;
    }
""")
```

替换为：
```python
self.setStyleSheet("""
    QDialog {
        background-color: #fafafa;
    }
    QLabel {
        color: #202124;
    }
    QProgressBar {
        border: none;
        border-radius: 4px;
        background-color: #e8eaed;
        text-align: center;
        color: #202124;
    }
    QProgressBar::chunk {
        background-color: #5f6368;
        border-radius: 4px;
    }
""")
```

- [ ] **Step 2: 更新步骤标签样式**

将 `#1d1d1f` 替换为 `#202124`，将 `#007aff` 替换为 `#5f6368`，将 `#8e8e93` 替换为 `#80868b`：

```python
self._step_label.setStyleSheet("font-size: 15px; font-weight: 700; color: #202124;")
self._name_label.setStyleSheet("font-size: 13px; color: #5f6368; font-weight: 600;")
self._instruction_label.setStyleSheet("font-size: 12px; color: #80868b;")
```

- [ ] **Step 3: 更新预览区域样式**

将：
```python
self._preview_label.setStyleSheet("background-color: #ffffff; border: 1px solid #d2d2d7; border-radius: 8px;")
```

替换为：
```python
self._preview_label.setStyleSheet("background-color: #ffffff; border: none; border-radius: 12px;")
```

- [ ] **Step 4: 更新按钮样式**

跳过按钮：
```python
self._skip_btn.setStyleSheet("background-color: #e8eaed; color: #202124; border: none; border-radius: 8px; padding: 8px 16px; font-weight: 500;")
```

截取按钮：
```python
self._capture_btn.setStyleSheet("background-color: #5f6368; color: #ffffff; border: none; border-radius: 8px; padding: 8px 16px; font-weight: 600;")
```

重截按钮：
```python
self._retake_btn.setStyleSheet("background-color: #e8eaed; color: #202124; border: none; border-radius: 8px; padding: 8px 16px; font-weight: 500;")
```

- [ ] **Step 5: 运行测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 全部通过

---

### Task 7: 更新 ScreenshotOverlay 样式

**Files:**
- Modify: `gui/screenshot_overlay.py`

- [ ] **Step 1: 更新工具栏容器样式**

将：
```python
self._toolbar.setStyleSheet(
    "background-color: #ffffff; border: 1px solid rgba(0, 0, 0, 0.1); border-radius: 8px; padding: 4px;"
)
```

替换为：
```python
self._toolbar.setStyleSheet(
    "background-color: #ffffff; border: none; border-radius: 12px; padding: 4px;"
)
```

- [ ] **Step 2: 更新尺寸标签样式**

将：
```python
self._size_label.setStyleSheet("color: #1d1d1f; font-size: 12px; font-weight: 500;")
```

替换为：
```python
self._size_label.setStyleSheet("color: #202124; font-size: 12px; font-weight: 500;")
```

- [ ] **Step 3: 更新确认按钮样式**

将：
```python
confirm_btn.setStyleSheet(
    "background-color: #007aff; color: #ffffff; border: none; "
    "border-radius: 4px; padding: 4px 12px; font-size: 12px; font-weight: 600;"
)
```

替换为：
```python
confirm_btn.setStyleSheet(
    "background-color: #5f6368; color: #ffffff; border: none; "
    "border-radius: 8px; padding: 4px 12px; font-size: 12px; font-weight: 600;"
)
```

- [ ] **Step 4: 更新取消按钮样式**

将：
```python
cancel_btn.setStyleSheet(
    "background-color: #e5e5ea; color: #1d1d1f; border: none; "
    "border-radius: 4px; padding: 4px 12px; font-size: 12px; font-weight: 500;"
)
```

替换为：
```python
cancel_btn.setStyleSheet(
    "background-color: #e8eaed; color: #202124; border: none; "
    "border-radius: 8px; padding: 4px 12px; font-size: 12px; font-weight: 500;"
)
```

- [ ] **Step 5: 更新选区边框颜色**

将 `paintEvent` 中的 `QColor(0, 122, 255)` 替换为 `QColor(95, 99, 104)` (#5f6368)。

- [ ] **Step 6: 运行测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 全部通过

---

### Task 8: 启动应用进行视觉验证

**Files:** 无代码变更

- [ ] **Step 1: 启动应用**

Run: `python main.py`

验证要点：
1. 窗口背景为 `#fafafa`（比之前的 `#f3f3f3` 更白）
2. 侧边栏无右边框，与背景同色
3. 播放/设置按钮为 28px 圆形，`#e0e0e0` 填充，无描边
4. 曲目卡片为白色，12px 圆角，无边框，有轻阴影
5. 歌单按钮为 `#e8eaed` 填充，8px 圆角
6. 所有文字颜色从 Apple 色系切换到 Google 灰色系
7. 网格间距从 2px 增大到 4px
